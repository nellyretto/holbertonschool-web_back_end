export default function getResponseFromAPI() {
  return new Promise(() => {});
}
// el error es cuando corre eslint ? si si
// ok, deja ver que puedo hacer we
// ya esta working dama
// happy coding !!
// hahah no hay de que we XD
